# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: 'nl'
    mailer_default_from_email: "%env(MAILER_DEFAULT_FROM_EMAIL)%"
    mailer_default_from_name: "%env(MAILER_DEFAULT_FROM_NAME)%"
    zoho.client_id: '1000.IDE5U5CS77V948467WHXY67CFUBGZR'
    zoho.client_secret: "d09e27dccb51dc003ee6c7f7ab5eb7793bc03e738d"
    zoho.redirect_uri: 'http://webdesigntilburg.nl'
    zoho.currentUserEmail: 'noud@webdesigntilburg.nl' # @TODO should be from logged in user
    zoho.log_path: '%kernel.project_dir%/var/zoho'
    zoho.api_base_url: "www.zohoapis.eu"
    zoho.accounts_url: "https://accounts.zoho.eu"
    zoho.grantToken: '1000.0f0764a8e73cb0afd0f47c0b7323b656.a2f4e8ee5d0fe4b30f79f9af49f12ea1'
    zoho.refreshToken: 'xx'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/*'
        exclude: '../src/{DependencyInjection,Entity,Migrations,Tests,Kernel.php}'

    # controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Controller\:
        resource: '../src/Controller'
        tags: ['controller.service_arguments']

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\Doctrine\HashPasswordListener:
        tags:
            - { name: doctrine.event_subscriber, connection: default }

    App\Service\MailerService:
        arguments:
            - "@mailer"
            - "@twig"
            - "%env(MAILER_DEFAULT_FROM_EMAIL)%"
            - "%env(MAILER_DEFAULT_FROM_NAME)%"
            
    # makes classes in src/DataFixtures available to be used as services
    # and have a tag that allows actions to type-hint services
    App\DataFixtures\:
        resource: '../src/DataFixtures'
        tags: ['doctrine.fixture.orm']

    App\Service\Zoho\ContactsService:
         arguments:
            - '%zoho.client_id%'
            - '%zoho.client_secret%'
            - '%zoho.redirect_uri%'
            - '%zoho.currentUserEmail%'
            - '%zoho.log_path%'
            - '%zoho.api_base_url%'
            - '%zoho.accounts_url'
            - '%zoho.grantToken%'
            - '%zoho.refreshToken%'