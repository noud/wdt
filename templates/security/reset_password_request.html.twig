{% extends 'base.html.twig' %}
{% trans_default_domain 'reset_password' %}

{% block navbar %}navbar-dark bg-dark border-bottom{% endblock %}
{% block title %}{{ 'reset_password.title.password_request'|trans }}{% endblock %}
{% block body %}
    <div class="main-content">
        <section class="slice slice-lg min-vh-100 d-flex align-items-center bg-gradient-dark">
            <div class="container py-5 px-md-0 d-flex align-items-center">
                <div class="w-100">
                    <div class="row row-grid justify-content-center justify-content-lg-between">
                        <div class="col-12 order-lg-2">
                            <div class="card shadow zindex-100 mb-0">
                                <div class="card-body px-md-5 py-5">
                                    <div class="mb-5">
                                        <h1 class="h1">{{ page.title }}</h1>
                                        <p class="text-muted mb-0">{{ page.content }}</p>
                                    </div>

                                    {% for error in form.vars.errors %}
                                        <div class="alert alert-error">
                                            {{ error.message }}
                                        </div>
                                    {% endfor %}
                                    {{ form_start(form, {'action': path('app_reset_password_request'), 'attr': {'autocomplete' : 'off'}}) }}
                                    
                                    {% include 'includes/_messages.html.twig' %}
        
                                    <div class="row">
                                        <div class="col-12 col-sm-4">
                                            {{ form_widget(form.username) }}
                                   		</div>
                                	</div>
                                    {{ form_row(form._token) }}
                                        <button type="submit" class="btn btn-sm btn-primary btn-icon rounded-pill">
                                            <span class="btn-inner--text">{{ 'reset_password.field.submit'|trans }}</span>
                                            <span class="btn-inner--icon"><i class="fas fa-long-arrow-alt-right"></i></span>
                                        </button>
                                    {{ form_end(form) }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
